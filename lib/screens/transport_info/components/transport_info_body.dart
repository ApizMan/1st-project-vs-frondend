import 'package:flutter/material.dart';

class TransportInfoBody extends StatelessWidget {
  const TransportInfoBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}